# Bloquea acceso web directo a archivos internos de includes/
# Compatible con Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Compatibilidad con Apache 2.2
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>

Options -Indexes
